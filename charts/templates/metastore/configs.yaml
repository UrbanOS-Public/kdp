apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "kdp.presto.fullname" . }}-metastore-configs
  labels:
    component: presto
{{ include "kdp.labels" . | indent 4 }}
data:
  jmx_exporter_config.yaml: |
    {}

  log.properties: |
    com.facebook.presto={{ .Values.presto.logLevel }}
